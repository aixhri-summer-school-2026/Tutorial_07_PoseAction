# Artifacts generated by the tutorial steps (reproducible, not committed).
/data/
/runs/

# Trained weights are generated by the training scripts. The provided
# pretrained hand detector is the exception and IS committed.
/weights/*.pt
!/weights/yolo26s-pose-hands.pt
