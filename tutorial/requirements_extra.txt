# Extra Python packages used by the tutorial that are NOT yet baked into the
# Docker image. For now we install them at runtime inside the running
# container (no rebuild) with:
#
#     uv pip install -r tutorial/requirements_extra.txt
#
# When we next rebuild the image, move these into the Dockerfile.
#
# Note: ultralytics, torch, torchvision, opencv-python and numpy are ALREADY in
# the image, so they are not listed here.

# Part 4 only: ONNX runtime for the SixDRepNet face detector (CPU build, which
# is reliable regardless of the host CUDA/cuDNN versions).
onnxruntime
